//se crea una interface para poder exportarla , se le agregan las propiedades y sus valores que tenemos en el json
export interface InfoPagina {

    titulo?: string;
    email?: string;
    nombre_corto?: string;
    pagina_autor?: string;
    facebook?: string;
    instagram?: string;
    equipo_trabajo?: any[];   
}

